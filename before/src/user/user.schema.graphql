type Query {
  User(id: String): User
}

type Mutation {
  createUser(
    type: Int,
    email: String,
    firstName: String,
    lastName: String,
    password: String,
  ): User
}

type User {
  id: String!
  email: String!
  firstName: String!
  lastName: String!
  password: String!
}